---
apiVersion: 1

datasources:
  - name: WaterAPIs
    type: grafoservice
    access: proxy
    withCredentials: false
    jsonData:
      url: ${WATERAPIS_URL}
    secureJsonData:
      token: ${WATERAPIS_TOKEN}
    version: 1
    editable: false
  - name: Influxservice
    type: influxdb
    access: proxy
    url: ${INFLUX_URL}
    secureJsonData:
      token: ${INFLUX_TOKEN}
    jsonData:
      version: Flux
      organization: ${INFLUX_ORG}
      defaultBucket: default
      tlsSkipVerify: true
    version: 1
    editable: false
